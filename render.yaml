services:
  - type: web
    name: attendance-software
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: NODE_VERSION
        value: 20.11.0
